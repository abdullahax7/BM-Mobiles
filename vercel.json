{
  "framework": "nextjs",
  "buildCommand": "rm -f prisma/schema.prisma && cp prisma/schema.production.prisma prisma/schema.prisma && npx prisma generate --schema=prisma/schema.prisma && npm run build",
  "outputDirectory": ".next",
  "installCommand": "npm install --force --legacy-peer-deps",
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next"
    }
  ]
}
