{
  "framework": "nextjs",
  "buildCommand": "cp prisma/schema.production.prisma prisma/schema.prisma && npx prisma generate && npm run build",
  "outputDirectory": ".next",
  "installCommand": "npm install --force",
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next"
    }
  ]
}
